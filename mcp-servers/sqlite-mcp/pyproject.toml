[project]
name = "sqlite-mcp-server"
version = "0.1.0"
description = "A local SQLite CRUD server using the Model Context Protocol"
readme = "README.md"
requires-python = "==3.13.15"
dependencies = [
    "mcp[cli]==1.30.0",
    "pydantic==2.13.5",
]

[tool.uv]
package = false
